{
  "variables": {
    "aws_region": "eu-central-1",
    "source_ami": "ami-0084a47cc718c111a",
    "instance_type": "t2.micro",
    "vpc_id": "vpc-0b82058702b96b919"
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "region": "{{user `aws_region`}}",
      "source_ami": "{{user `source_ami`}}",
      "instance_type": "{{user `instance_type`}}",
      "ssh_username": "ubuntu",
      "ami_name": "flask-app-{{timestamp}}",
      "vpc_id": "{{user `vpc_id`}}",
      "subnet_id": "subnet-0c6a5cbfad1bea9bf"
    }
  ],
 "provisioners": [
    {
      "type": "ansible",
      "playbook_file": "../ansible/app_install.yml"
    }
  ]
}
